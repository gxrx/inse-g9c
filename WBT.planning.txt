Drawing to a Java FX window - or canvas

Variables to have.
windowMidX
windowTopY

1) add the project title in a box. Top Middle of window.

Rectangle rectTitle = new Rectangle(midX, topY, boxWidth, boxHight);

add text to this box.
addTitletoBox(rectangle)

2) split the window into virtual columns being the number of unique first character ID.
getNumUniqId(project) //returns the number of branches we're dealing with.
windowWidth / uniqEvents = column width 

3) draw events
order project events() --eventList to be ordered as (1.0, 1.1, 2.0, 2.1, 2.1.1, 3.0 etc...)

for each unique ID
	for each event 
		see if the event is a sub tree.
		draw a rect (column width x the unique ID) - (column width / 2), prevHeight)
		addTitletoBox(rectangle, event.getid, event.getname)
		addArrow(this-1 or TITLE, this)
		prevHeight =+ 30 (space things out)
		addTitleToBox(rectangle, event.getID, event.getName();
		addArrow(this-1 or TITLE, this)
	prevheight = 100






